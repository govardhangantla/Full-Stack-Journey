# 🚀 JavaScript Journey — Return Values

This section covers **JavaScript Return Values** from **Lesson 31**.

The `return` statement allows a function to send a value back to the place where the function was called.

---

## 📚 Lesson 31 — Return Values

### Topics Covered

- `return` statement
- Returning numbers
- Returning strings
- Returning Boolean values
- Returning results from loops
- Storing returned values
- Using returned values in calculations
- Returning values from conditions

---

# 🧠 What is `return`?

The `return` statement sends a value back from a function.

### Example

```javascript
function add(a, b) {
    return a + b;
}

let result = add(10, 20);

console.log(result);
